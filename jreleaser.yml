signing:
  active: NEVER
  armored: true

release:
  github:
    enabled: true
    skipTag: true
    skipRelease: true
    uploadAssets: NEVER
    owner: alapierre
    name: ksef-client-java-mf-fork

deploy:
  maven:
    mavenCentral:
      app:
        active: ALWAYS
        sign: false
        url: https://central.sonatype.com/api/v1/publisher
        stagingRepositories:
          - ./build/staging-deploy